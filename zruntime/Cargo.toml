[package]
name = "zruntime"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
futures = "0.3.0"
rustix = { version = "1.1.2", default-features = false, features = ["std", "event"] }
