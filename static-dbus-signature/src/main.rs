#![allow(dead_code, unused)]

mod dynamic_type;
mod signature;
mod structure;
mod r#type;

use structure::Structure;

fn main() {
    //let sig = Structure::new().field::<i32>().field::<&[&[i32]]>();

    //println!("{}", sig.as_str());
}
